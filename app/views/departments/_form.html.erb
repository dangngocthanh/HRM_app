<%= form_with(model: @department, local: true) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :user_id %>
  <%= f.collection_select(:user_id, @users, :id, :name, prompt: false )%>
  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>